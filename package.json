{
  "name": "eriq",
  "private": true,
  "description": "Emotional Resonance Intelligence Quotient — diagnostic tool for brand/campaign emotional impact",
  "scripts": {
    "dev": "npm run build -w shared && concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build -w shared && npm run build --workspace=client --workspace=server --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "client",
    "server",
    "shared"
  ]
}
